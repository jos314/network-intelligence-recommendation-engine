# Floors only, no ceilings — installs whatever recent versions your package
# source carries (the app is verified against pandas 2.x / numpy 2.x too).
# If a specific package can't be resolved on a restricted mirror, install the
# rest and pin that one to the newest version the mirror offers.
pandas>=2.0
numpy>=1.24
networkx>=3.0
scipy>=1.10
scikit-learn>=1.3
dash>=2.14
dash-cytoscape>=0.3
pyarrow>=12
pytest
